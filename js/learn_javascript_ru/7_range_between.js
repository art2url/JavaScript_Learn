//Check the range between

//Write an if condition to check that age 
//is between 14 and 90 inclusively.

//“Inclusively” means that age can reach the edges 14 or 90.

let age;
age = prompt('Input number', '');
if (age >= 14 && age <= 90) {
  alert(age);
} else {
  alert("You're in not diapason 14-90");
}